/** Dummy route backing the raised center tab button; the button navigates to
 *  /trip/new instead of focusing this screen, so it never renders. */
export default function CreatePlaceholderRoute() {
  return null;
}
