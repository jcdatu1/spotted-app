/**
 * Placeholder for generated database types.
 *
 * Once the first migration lands (add-auth-and-profiles), replace this file's
 * Database type with the output of:
 *
 *   npx supabase gen types typescript --local > src/data/types.ts
 *
 * No tables exist in the scaffold, so the schema is intentionally empty.
 */
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
    CompositeTypes: Record<string, never>;
  };
};
